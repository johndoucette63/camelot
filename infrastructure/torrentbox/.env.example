# Torrentbox sidecar credentials — TEMPLATE
#
# Copy this file to /home/john/docker/.env on the Pi and fill in the real
# values. The real .env file lives ONLY on the Pi and is gitignored.
# Never commit credentials.

# PIA account credentials.
# These are the same username/password historically used by openvpn@pia
# (previously stored in /etc/openvpn/pia-credentials.txt on the Pi).
OPENVPN_USER=p1234567
OPENVPN_PASSWORD=replace-with-real-password
