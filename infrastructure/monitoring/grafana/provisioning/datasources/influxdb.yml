apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    url: http://influxdb:8086
    database: network_metrics
    user: $INFLUXDB_USER
    secureJsonData:
      password: $INFLUXDB_USER_PASSWORD
    isDefault: true
    editable: false
